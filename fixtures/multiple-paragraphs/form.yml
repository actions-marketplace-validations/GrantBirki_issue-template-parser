# this isn't used in tests and is just a reference

body:
  - type: textarea
    id: textarea-one
    attributes:
      label: My textarea input
  - type: textarea
    id: textarea-two
    attributes:
      label: Another textarea input
